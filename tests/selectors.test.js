describe(`Overview ofelectors`, () => {
    it(`should verify selectors`, () => {
        browser.url(`https://the-internet.herokuapp.com/`);

        //$(`h1.heading`).getText();
        //$(`heading`).getText();

        //$(`h2=Available Examples`).getText()

        //$(`a=Broken Images`).click();
        //$(`a*=Images`).click();

        //$(`ul`).$(`..`).getText();
        //$(`ul`).$(`a=Checkboxes`).click();
    });
});